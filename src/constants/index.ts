// Bootstrap fallback only: used as the initial selected season when nothing is
// stored in localStorage. The real season list (and which one is active) comes
// from the Firestore `seasons` collection via the season store.
export const SEASON = '2025-2026';
export const TEAMNAME = 'JO9';
export const CLUBNAME = "SV Apollo '69";
export const TOAST_LIFE = 3000;
